import { Button } from '@/shared/ui/button'

const NotFound: React.FC = () => {
  return (
    <div>
      <div>Login page</div>
      <Button onClick={() => alert('Test button')}>Test</Button>
    </div>
  )
}

export default NotFound
